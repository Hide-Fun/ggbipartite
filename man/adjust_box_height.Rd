% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global_layout.R
\name{adjust_box_height}
\alias{adjust_box_height}
\title{Adjust per-box vertical gaps to equalize heights}
\usage{
adjust_box_height(.interaction, .nrow, .ncol, .gap)
}
\arguments{
\item{.interaction}{Numeric scalar; the total mass (typically `sum(.mat)`).}

\item{.nrow, .ncol}{Positive integers; number of rows/columns in `.mat`.}

\item{.gap}{Baseline gap used for whichever box is treated as fixed.}
}
\value{
A named list with elements:
\itemize{
  \item `.gap1`: gap for box 1 (rows).
  \item `.gap2`: gap for box 2 (columns).
}
If the two heights are already equal, the function only emits a message and
returns `NULL`.
}
\description{
Given the total mass (`.interaction`), matrix dimensions, and a baseline gap,
compute per-box gaps (`.gap1`, `.gap2`) so that the total heights for the
two boxes are equal. If the heights are already equal, a message is emitted
and no value is returned.
}
\examples{
adjust_box_height(.interaction = 10, .nrow = 4, .ncol = 3, .gap = 0.2)

}
