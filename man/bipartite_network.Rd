% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bipartite_network.R
\name{bipartite_network}
\alias{bipartite_network}
\title{Summarize a bipartite interaction matrix}
\usage{
bipartite_network(.mat)
}
\arguments{
\item{.mat}{A numeric matrix representing a bipartite interaction network.
Rows and columns correspond to the two disjoint node sets; entries are
interaction magnitudes (e.g., counts, weights).}
}
\value{
A list with three tibbles:
\itemize{
\item \code{rsf}: row-sum features with columns \code{row} (row index) and
\code{interaction_size} (sum over the row).
\item \code{csf}: column-sum features with columns \code{column} (column index) and
\code{interaction_size} (sum over the column).
\item \code{ilf}: long-form interactions as returned by \code{\link[=to_longer]{to_longer()}}, typically
containing row/column identifiers and a value column.
}
}
\description{
Compute row- and column-wise interaction totals and a long-form version of
the bipartite interaction matrix.
}
\details{
This function is a light-weight helper that:
\enumerate{
\item Computes row totals via \code{\link[base:rowSums]{base::rowSums()}} and wraps them with
\code{\link[tibble:enframe]{tibble::enframe()}}.
\item Computes column totals via \code{\link[base:colSums]{base::colSums()}} and wraps them with
\code{\link[tibble:enframe]{tibble::enframe()}}.
\item Delegates reshaping to the package's \code{\link[=to_longer]{to_longer()}} helper, which
applies the same strict matrix contract and returns a long tibble.
}
}
\section{Input validation}{

\code{.mat} must be a numeric matrix with finite, non-negative values and
non-empty, unique row and column names. Every row and column must have a
positive total interaction weight. Invalid IDs and zero-total nodes are
reported before coordinates are computed.
}

\examples{
m <- matrix(c(1, 0, 2,
              3, 1, 1), nrow = 2, byrow = TRUE)
dimnames(m) <- list(c("row_1", "row_2"), c("col_1", "col_2", "col_3"))

bip <- bipartite_network(m)
bip$rsf
bip$csf
bip$ilf

}
