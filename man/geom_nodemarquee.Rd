% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_nodemarquee.R
\name{geom_nodemarquee}
\alias{geom_nodemarquee}
\title{Draw Node Labels with \code{marquee}}
\usage{
geom_nodemarquee(
  mapping = NULL,
  data = NULL,
  nudge_x = 0,
  nudge_y = 0,
  geom = "text",
  hjust = 0.5,
  node = "internal",
  align = FALSE,
  offset = 0,
  segment = align,
  segment_linetype = "dotted",
  segment_linewidth = 0.5,
  stat = "identity",
  position = "identity",
  ...,
  size.unit = "mm",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings.}

\item{data}{A data frame, \code{NULL}, or function evaluated by \code{ggplot2}.}

\item{nudge_x}{Numeric scalar x-offset, compatible with
\code{ggtree::geom_nodelab()}.}

\item{nudge_y}{Numeric scalar y-offset, compatible with
\code{ggtree::geom_nodelab()}.}

\item{geom}{Character scalar. Only \code{"text"} is supported for compatibility
with \code{ggtree::geom_nodelab()}.}

\item{hjust}{Horizontal justification passed to \code{marquee::geom_marquee()}.}

\item{node}{Character scalar, one of \code{"internal"}, \code{"external"}, or \code{"all"}.}

\item{align}{Logical scalar. If \code{TRUE}, selected labels share one x-position.}

\item{offset}{Numeric scalar subtracted from label x-position.}

\item{segment}{Logical scalar. If \code{TRUE}, draw guide segments.}

\item{segment_linetype}{Linetype for guide segments.}

\item{segment_linewidth}{Line width for guide segments.}

\item{stat}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{position}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{...}{Additional arguments passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{size.unit}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{na.rm}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{show.legend}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{inherit.aes}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}
}
\value{
A list of ggplot2 layers.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

API-compatible wrapper around \code{ggtree::geom_nodelab()}-style node selection
and \code{marquee::geom_marquee()} rendering. It can optionally align selected
node labels to a shared left x-position and draw guide segments.
}
\examples{
if (
  requireNamespace("ape", quietly = TRUE) &&
    requireNamespace("marquee", quietly = TRUE)
) {
  tree <- ape::read.tree(text = "((A:1,B:1):1,C:2);")
  tree$node.label <- c("90", "100")

  ggtree::ggtree(tree) +
    geom_nodemarquee(
      mapping = ggplot2::aes(label = label),
      align = TRUE,
      hjust = 1
    ) +
    ggplot2::coord_cartesian(clip = "off")
}
}
