% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_tipmarquee.R
\name{geom_tipmarquee}
\alias{geom_tipmarquee}
\title{Draw Tip Labels with \code{marquee}}
\usage{
geom_tipmarquee(
  mapping = NULL,
  data = NULL,
  hjust = 0,
  align = FALSE,
  linetype = "dotted",
  linesize = 0.5,
  geom = "text",
  offset = 0,
  as_ylab = FALSE,
  stat = "identity",
  position = "identity",
  ...,
  size.unit = "mm",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings.}

\item{data}{A data frame, \code{NULL}, or function evaluated by \code{ggplot2}.}

\item{hjust}{Horizontal justification passed to \code{marquee::geom_marquee()}.}

\item{align}{Logical scalar. If \code{TRUE}, tip labels are aligned to a shared
right x-position and guide segments are drawn.}

\item{linetype}{Linetype for guide segments when \code{align = TRUE}.}

\item{linesize}{Line width for guide segments when \code{align = TRUE}.}

\item{geom}{Character scalar. Only \code{"text"} is supported for compatibility
with \code{ggtree::geom_tiplab()}.}

\item{offset}{Numeric scalar added to the tip label x-position.}

\item{as_ylab}{Logical scalar from \code{ggtree::geom_tiplab()}. Only \code{FALSE} is
supported.}

\item{stat}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{position}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{...}{Additional arguments passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{size.unit}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{na.rm}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{show.legend}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}

\item{inherit.aes}{Passed to \code{\link[marquee:geom_marquee]{marquee::geom_marquee()}}.}
}
\value{
A list of ggplot2 layers.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

API-compatible wrapper around \code{ggtree::geom_tiplab()}-style arguments and
\code{marquee::geom_marquee()} rendering.
}
\examples{
if (
  requireNamespace("ape", quietly = TRUE) &&
    requireNamespace("marquee", quietly = TRUE)
) {
  tree <- ape::read.tree(text = "((A:1,B:1):1,C:2);")

  ggtree::ggtree(tree) +
    geom_tipmarquee(align = TRUE, hjust = 0) +
    ggplot2::coord_cartesian(clip = "off")
}
}
