% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bipartite-layout.R
\name{layout_bipartite}
\alias{layout_bipartite}
\title{Compute a reusable bipartite layout}
\usage{
layout_bipartite(
  data,
  row = NULL,
  column = NULL,
  weight = NULL,
  interaction = c("abundance", "binary"),
  row_tree = NULL,
  column_tree = NULL,
  metadata_row = NULL,
  metadata_column = NULL,
  metadata_row_key = NULL,
  metadata_column_key = NULL,
  duplicate = NULL,
  unmatched_data = c("error", "drop"),
  unmatched_tree = c("error", "prune"),
  x0 = 0,
  y0 = 0,
  gap = NULL,
  box_ratio = 5,
  ratio = 1/1.618,
  adjust_box_height = FALSE
)
}
\arguments{
\item{data}{A numeric matrix with non-empty, unique dimnames, or a data
frame in long format.}

\item{row, column, weight}{Columns in a long-format \code{data} frame. Supply bare
names or single strings. These arguments must be \code{NULL} for matrix input.}

\item{interaction}{One of \code{"abundance"} or \code{"binary"}. Binary mode treats
every positive weight as presence; the mode is never inferred from values.}

\item{row_tree, column_tree}{Optional \code{phylo} objects for the row and column
partitions.}

\item{metadata_row, metadata_column}{Optional node metadata data frames.}

\item{metadata_row_key, metadata_column_key}{Single strings identifying the
metadata key columns. For long data, these default to the corresponding
\code{row} or \code{column} column name. For matrix data, they default to \code{"row"}
and \code{"column"}.}

\item{duplicate}{Optional function used to aggregate duplicate long-format
row-column cells. The default, \code{NULL}, rejects duplicate cells.}

\item{unmatched_data}{What to do with data IDs absent from a supplied tree:
\code{"error"} (the default) or \code{"drop"}.}

\item{unmatched_tree}{What to do with tree tips absent from the data:
\code{"error"} (the default) or \code{"prune"}. Pruning requires the suggested
package \code{ape}.}

\item{x0, y0}{Finite numeric scalars defining the network origin.}

\item{gap}{A finite non-negative gap between nodes. When \code{NULL}, a value of
one tenth of the total interaction weight is used.}

\item{box_ratio}{A positive finite divisor controlling node-box width.}

\item{ratio}{A positive finite divisor controlling total network width.}

\item{adjust_box_height}{Whether to adjust side-specific gaps so both
partitions have the same total height.}
}
\value{
A \code{bipartite_layout} object with stable components \code{nodes},
\code{interactions}, \code{tree_links}, \code{params}, and \code{trees}.
}
\description{
\code{layout_bipartite()} validates and normalizes a bipartite interaction input
once, then computes coordinates shared by all ggbipartite layers. Matrix
inputs use their dimnames as identifiers. Long data inputs require explicit
\code{row}, \code{column}, and \code{weight} columns.
}
\examples{
interaction_matrix <- matrix(
  c(2, 0, 1, 3),
  nrow = 2,
  dimnames = list(c("host_a", "host_b"), c("otu_a", "otu_b"))
)
layout <- layout_bipartite(interaction_matrix)
layout

interaction_data <- tibble::tibble(
  host = c("host_a", "host_a", "host_b"),
  otu = c("otu_a", "otu_b", "otu_b"),
  abundance = c(2, 1, 3)
)
layout_bipartite(
  interaction_data,
  row = host,
  column = otu,
  weight = abundance
)

}
