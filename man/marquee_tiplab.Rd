% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marquee_tiplab.R
\name{marquee_tiplab}
\alias{marquee_tiplab}
\title{marquee_tiplab}
\usage{
marquee_tiplab(
  p_tree,
  align,
  label_map,
  size,
  width_mm = 28,
  show_guides = TRUE,
  guide_linetype = "dotted",
  guide_linesize = 0.5,
  x_start_nudge = 0.1,
  y_start_nudge = 0,
  guide_end_gap_ratio = 0.03,
  ...
)
}
\arguments{
\item{p_tree}{A `ggtree`/`ggplot` object containing the tree to annotate.}

\item{align}{Value passed to [ggtree::geom_tiplab()]'s `align` argument while
probing tip positions (typically `TRUE` or a numeric offset).}

\item{label_map}{Data frame with columns `old` (original labels) and `new`
(replacement labels) used to remap tip labels.}

\item{size}{Text size for marquee labels, forwarded to
[marquee::geom_marquee()].}

\item{width_mm}{Width of each marquee label in millimetres.}

\item{show_guides}{Logical; draw guide line segments from tips to marquee
labels when `TRUE`.}

\item{guide_linetype}{Linetype used for the guide segments.}

\item{guide_linesize}{Line width used for the guide segments.}

\item{x_start_nudge}{Horizontal nudge (as a fraction of the tree width) that
offsets the marquee labels from the aligned tip positions.}

\item{y_start_nudge}{Vertical nudge (as a fraction of the tree height) that
offsets the marquee labels.}

\item{guide_end_gap_ratio}{Ratio of the diagonal span that determines how far
each guide line stops before reaching the label start.}

\item{...}{Additional arguments passed to [marquee::geom_marquee()].}
}
\value{
A `ggplot` object with marquee labels applied to `p_tree`.
}
\description{
Use an already-built ggtree object as the base instead of a phylo.
This function probes aligned tip label positions by temporarily adding
geom_tiplab(align = ...) and then draws marquee labels (and guide segments).
}
