% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-bipartite.R
\name{plot_bipartite}
\alias{plot_bipartite}
\title{Compose a bipartite network and optional phylogenetic trees}
\usage{
plot_bipartite(
  data,
  row = NULL,
  column = NULL,
  weight = NULL,
  widths = NULL,
  ...
)
}
\arguments{
\item{data}{A matrix or long data frame accepted by \code{\link[=layout_bipartite]{layout_bipartite()}},
or an existing \code{bipartite_layout} object.}

\item{row, column, weight}{Columns used for long-format raw data. Supply bare
names or single strings. They must not be supplied when \code{data} is already
a \code{bipartite_layout}.}

\item{widths}{Optional panel widths. Supply either one positive value per
active component or a named positive vector using any of \code{row_tree},
\code{row_link}, \code{network}, \code{column_link}, and \code{column_tree}.}

\item{...}{Additional arguments passed to \code{\link[=layout_bipartite]{layout_bipartite()}} for raw
input. Additional arguments are not accepted for a precomputed layout.}
}
\value{
A \code{ggbipartite_plot} object with named \code{components}, the reusable
\code{layout}, and resolved panel \code{widths}. Missing tree and link components
are represented by \code{NULL}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
\code{plot_bipartite()} builds editable ggplot components from either raw
interaction data or a reusable \code{\link[=layout_bipartite]{layout_bipartite()}} result. Raw data are
normalized and laid out exactly once. When a layout is supplied, its
validated trees and fixed coordinates are reused without recomputation.

The returned object is deliberately not a patchwork object. Its named
components can be inspected or edited before \code{\link[=as_patchwork]{as_patchwork()}} assembles
them. The suggested package \code{patchwork} is required only when two or more
non-\code{NULL} panels must be assembled.

With trees, every panel uses the same y scale and fills the same vertical
space. Panel widths change horizontal spacing without moving matched tips
and nodes vertically. Tree-free plots retain a fixed coordinate aspect
ratio. The composer does not change the stored layout or tree coordinates.
}
\examples{
interaction_matrix <- matrix(
  c(2, 0, 1, 3),
  nrow = 2,
  dimnames = list(c("host_a", "host_b"), c("otu_a", "otu_b"))
)
composed <- plot_bipartite(interaction_matrix)
composed$components$network

precomputed <- layout_bipartite(interaction_matrix)
plot_bipartite(precomputed)

}
